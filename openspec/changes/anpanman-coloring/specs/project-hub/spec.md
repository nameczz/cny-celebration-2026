## MODIFIED Requirements

### Requirement: 主页展示项目列表
主页 SHALL 以卡片形式展示所有子项目的入口，每张卡片包含项目标题、简短描述和封面图。

#### Scenario: 用户访问主页
- **WHEN** 用户访问根路径 `/`
- **THEN** 页面展示所有子项目的卡片列表，按年份分组显示，包含面包超人填色游戏卡片

#### Scenario: 卡片包含必要信息
- **WHEN** 主页渲染完成
- **THEN** 每张项目卡片 MUST 包含项目标题、简短描述文字，以及可视化的封面区域
