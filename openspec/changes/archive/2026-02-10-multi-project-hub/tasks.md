## 1. 迁移现有项目到年份目录

- [x] 1.1 创建 `2026/cny/` 目录
- [x] 1.2 将 `index.html`、`style.css`、`script.js` 移入 `2026/cny/`
- [x] 1.3 将 `fonts/`、`music/` 目录移入 `2026/cny/`
- [x] 1.4 检查 `2026/cny/index.html` 中的资源引用路径，确保使用相对路径（`style.css`、`script.js`、`fonts/`、`music/`）

## 2. 更新 Vercel 配置

- [x] 2.1 更新 `vercel.json`，移除显式 builds 配置，添加年份目录路由规则（`/(\\d{4})/([^/]+)/?$` → `/$1/$2/index.html`）

## 3. 更新根目录 package.json

- [x] 3.1 更新 `package.json` 的 `scripts`，添加 `build` 脚本（遍历子目录，检测有 `package.json` 的项目执行构建）
- [x] 3.2 更新 `dev` 脚本，确保本地开发服务器能正确服务多目录结构

## 4. 创建主页

- [x] 4.1 创建根目录 `index.html` 主页，包含按年份分组的项目卡片列表
- [x] 4.2 创建根目录 `style.css` 主页样式，卡片布局和响应式适配
- [x] 4.3 添加 CNY 2026 项目卡片（标题、描述、封面、链接到 `/2026/cny/`）

## 5. 验证

- [x] 5.1 本地启动服务器，验证主页正确显示项目列表
- [x] 5.2 验证点击卡片能正确跳转到 `/2026/cny/`
- [x] 5.3 验证 `/2026/cny/` 页面所有功能正常（烟花、抽签、红包、音乐）
- [x] 5.4 验证 `/2026/cny`（无尾部斜杠）也能正确访问
